---
const navLinks = [
  { href: '/for-parents',       label: 'For Parents' },
  { href: '/for-practitioners', label: 'For Practitioners' },
  { href: '/about',             label: 'About Us' },
  { href: '/start-here',        label: 'Resources' },
];
const current = Astro.url.pathname;
---

<header
  id="nsh-header"
  class="fixed top-0 left-0 right-0 z-50 transition-all duration-300"
  style="background: rgba(255,255,255,0.92); backdrop-filter: blur(12px); border-bottom: 1px solid transparent;"
>
  <div class="container">
    <div class="flex items-center justify-between h-16">

      <!-- Logo -->
      <a href="/" class="flex items-center gap-2.5 shrink-0">
        <img
          src="/logo-blue.png"
          alt="Neurosage Hub"
          class="h-9 w-auto"
          id="nav-logo"
          onerror="this.style.display='none'; document.getElementById('logo-fallback').style.display='block'"
        />
        <span
          id="logo-fallback"
          style="display:none; font-family:'DM Sans',sans-serif; color:oklch(0.46 0.22 258); font-size:1.125rem; font-weight:800;"
        >
          Neurosage Hub
        </span>
      </a>

      <!-- Desktop Nav -->
      <nav class="hidden lg:flex items-center gap-1">
        {navLinks.map(({ href, label }) => (
          <a
            href={href}
            class="px-4 py-2 rounded-lg text-sm transition-all duration-200"
            style={`font-family:'DM Sans',sans-serif; color:${current.startsWith(href) ? 'oklch(0.46 0.22 258)' : 'oklch(0.35 0.06 258)'}; background:${current.startsWith(href) ? 'oklch(0.95 0.04 258)' : 'transparent'}; font-weight:${current.startsWith(href) ? '600' : '500'};`}
          >
            {label}
          </a>
        ))}
      </nav>

      <!-- CTA + mobile toggle -->
      <div class="flex items-center gap-3">
        <a
          href="/start-here"
          class="hidden md:inline-flex nsh-btn-primary"
          style="font-size:0.875rem; padding:0.5rem 1.25rem;"
        >
          Get Started Free
        </a>
        <button
          id="mobile-toggle"
          class="lg:hidden p-2 rounded-lg"
          style="color:oklch(0.46 0.22 258);"
          aria-label="Toggle menu"
        >
          <svg id="icon-menu" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
          <svg id="icon-close" class="hidden" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div
    id="mobile-menu"
    class="lg:hidden hidden border-t"
    style="background:rgba(255,255,255,0.98); border-color:oklch(0.90 0.03 258);"
  >
    <div class="container py-4 flex flex-col gap-1">
      {navLinks.map(({ href, label }) => (
        <a
          href={href}
          class="px-4 py-3 rounded-xl text-sm font-medium"
          style="font-family:'DM Sans',sans-serif; color:oklch(0.35 0.06 258);"
        >
          {label}
        </a>
      ))}
      <a href="/start-here" class="nsh-btn-primary mt-2 justify-center">
        Get Started Free
      </a>
    </div>
  </div>
</header>

<div class="h-16"></div>

<script>
  const header = document.getElementById('nsh-header');
  window.addEventListener('scroll', () => {
    if (window.scrollY > 20) {
      header.style.background = 'rgba(255,255,255,0.97)';
      header.style.borderBottomColor = 'oklch(0.90 0.03 258)';
      header.style.boxShadow = '0 2px 16px oklch(0.22 0.12 258 / 0.07)';
    } else {
      header.style.background = 'rgba(255,255,255,0.92)';
      header.style.borderBottomColor = 'transparent';
      header.style.boxShadow = 'none';
    }
  }, { passive: true });

  const toggle = document.getElementById('mobile-toggle');
  const menu = document.getElementById('mobile-menu');
  const iconMenu = document.getElementById('icon-menu');
  const iconClose = document.getElementById('icon-close');
  toggle.addEventListener('click', () => {
    const isHidden = menu.classList.toggle('hidden');
    iconMenu.classList.toggle('hidden', !isHidden);
    iconClose.classList.toggle('hidden', isHidden);
  });

  const observer = new IntersectionObserver(
    (entries) => entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); }),
    { threshold: 0.08, rootMargin: '0px 0px -30px 0px' }
  );
  document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));
</script>
